---
summary: "CLI reference for `clawdia uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `clawdia uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
clawdia uninstall
clawdia uninstall --all --yes
clawdia uninstall --dry-run
```

